schema_version: 1
claims:
  - id: "MERCHANT-PERMIT-OPEN-001"
    statement: >-
      It remains open whether adaptive pre-call allocation of enforceable time, token,
      API-call, and API-spend permits improves purchase loss or purchase success over
      fixed-split and myopic feasible baselines while maintaining zero hard-budget
      violations under uncertain realized query usage.
    status: "open"
    scope: >-
      Sequential no-recall merchant search with enforceable per-call limits, uncertain
      resource usage, a hard purchase-price cap, and paired replay traces.
    evidence:
      - "research/literature.yml"
    manuscript_labels: []
    site_visible: true
    limitations:
      - "The repository does not yet contain a calibrated query-usage trace corpus."
      - "Zero violations require the host runtime to enforce every permit before invoking a model or merchant tool."
