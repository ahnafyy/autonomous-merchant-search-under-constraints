# Evidence Gate

Approve only when executable claims pass, analytic claims have inspectable proof
locations, computational patterns state their scanned domain, failed hypotheses
are retained where they affect interpretation, and a falsification review has no
unresolved blocking finding.

## Current Evidence

- The Python middleware atomically reserves vector permits, prevents repeated
	reconciliation, reclaims known unused capacity, and charges censored components at
	their permit values; focused tests cover these implementation invariants.
- Commerce-native exact-SKU and price types, frozen-panel exhaustive-oracle metrics,
	and offline UCP endpoint eligibility screening are implemented and unit tested.
- Python and JavaScript still conform for the existing planner and completed-call
	operations. JavaScript reservation receipts and censored reconciliation are pending.
- Deterministic build, package conformance, unit tests, static-site checks, and
	desktop/mobile interaction and accessibility checks pass.
- Existing hard-constraint scenarios are implementation validation only.

## Approval Blockers

- No permission-checked UCP endpoint inventory, exact-SKU cohort, calibration corpus,
	or frozen held-out panel corpus has been added.
- The exact joint merchant-and-permit solver, exhaustive reduced-domain enumeration,
	ARVP policy, declared feasible baselines, and JavaScript receipt lifecycle are not
	implemented.
- No constrained, nonbinding-budget, and exhaustive-oracle policy comparison has been
	evaluated on paired held-out panels.
- `MERCHANT-PERMIT-SAFETY-CONJECTURE-001` lacks a manuscript proof and remains a
	conjecture; implementation tests are not proof.
- `MERCHANT-PERMIT-SOLVER-OPEN-001` remains open.
- `MERCHANT-PERMIT-OPEN-001` remains open.
- An independent falsification review has not been completed.

Record approval in `status.yml`. Agents may prepare this gate but may not approve it.
